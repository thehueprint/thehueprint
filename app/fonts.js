import { Cuprum, Maitree } from '@next/font/google';

export const maitree = Maitree({
  weight: 400,
  subsets: ['latin'],
  display: 'optional',
});

export const cuprum = Cuprum({
  subsets: ['latin'],
  display: 'optional',
});
